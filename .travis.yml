language: cpp
compiler: gcc
dist: bionic

# The available apt packages can be found here:
# https://packages.ubuntu.com/bionic/allpackages
addons:
  apt:
    packages:
      - libpetsc-real3.11

script:
 - pwd
# - ./tests/travis.sh
